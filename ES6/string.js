// the new `` and ${const} 
//there you can run a function or any operation like this case: calculation of dog yearas and 7 

const name = "Snicker"
const age = 2;
const sentence = `My dog ${name} is ${age * 7 } years old`

// Functions passing 

function highlight (strings, ...values){

}

const sentence2 = highlight `My dog ${name} is ${age * 7 } years old`